{
  "name": "@pipcook/plugins-tfjs-text-model-evaluate",
  "version": "0.5.9",
  "description": "",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "author": "",
  "license": "Apache 2.0",
  "dependencies": {
    "@pipcook/pipcook-core": "^0.5.16",
    "@tensorflow/tfjs-node-gpu": "1.7.0"
  },
  "devDependencies": {
    "@types/jasmine": "^3.5.7",
    "nyc": "14.1.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "pipcook": {
    "category": "modelEvaluate",
    "datatype": "text"
  }
}
