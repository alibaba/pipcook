{
  "name": "@pipcook/plugins-image-generate-tensorflow-model-evaluate",
  "version": "0.6.3",
  "description": "",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "author": "feely",
  "license": "Apache-2.0",
  "dependencies": {
    "@pipcook/boa": "^0.6.0",
    "@pipcook/pipcook-core": "^0.6.3"
  },
  "devDependencies": {
    "@types/jasmine": "^3.5.7",
    "nyc": "14.1.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "pipcook": {
    "category": "modelEvaluate",
    "datatype": "image"
  }
}
